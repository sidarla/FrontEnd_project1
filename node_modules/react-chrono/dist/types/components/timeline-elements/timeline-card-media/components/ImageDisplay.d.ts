import { default as React } from 'react';
import { TimelineMode } from '../../../../models/TimelineModel';
interface ImageDisplayProps {
    url: string;
    name: string;
    mode: TimelineMode;
    mediaLoaded: boolean;
    borderLessCards: boolean;
    mediaHeight: number;
    imageFit?: string;
    handleMediaLoaded: () => void;
    handleError: () => void;
}
/**
 * Renders an image with appropriate loading and error handling
 */
export declare const ImageDisplay: React.MemoExoticComponent<({ url, name, mode, mediaLoaded, borderLessCards, mediaHeight, imageFit, handleMediaLoaded, handleError, }: ImageDisplayProps) => import("react/jsx-runtime").JSX.Element>;
export {};
//# sourceMappingURL=ImageDisplay.d.ts.map